name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  quality:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
      - name: Install package and quality tools
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[sqlalchemy,jwt,oidc,oidc-http,mfa,fastapi,django]"
          python -m pip install build "django-stubs>=6.1,<6.2" httpx mypy pytest pytest-cov ruff
      - name: Lint
        run: ruff check .
      - name: Format check
        run: ruff format --check .
      - name: Type check
        run: mypy src
      - name: Tests and coverage
        run: pytest --cov=pyiamkit --cov-report=term-missing --cov-fail-under=90
      - name: Build
        run: python -m build
      - name: Install wheel
        run: python -m pip install --force-reinstall dist/*.whl
      - name: Smoke import
        run: python scripts/smoke_install.py
      - name: Run examples
        run: |
          for example in examples/*.py; do
            python "$example"
          done

  postgres-conformance:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: pyiamkit
          POSTGRES_PASSWORD: pyiamkit
          POSTGRES_DB: pyiamkit_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U pyiamkit -d pyiamkit_test"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      PYIAMKIT_TEST_DATABASE_URL: postgresql+psycopg://pyiamkit:pyiamkit@localhost:5432/pyiamkit_test
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - name: Install PostgreSQL persistence extra
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[postgres]"
          python -m pip install pytest
      - name: PostgreSQL persistence integration tests
        run: pytest tests/integration/persistence/test_postgresql.py -m integration -q


  django-conformance:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        django-spec:
          - "Django>=5.2,<5.3"
          - "Django>=6.1,<6.2"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - name: Install Django compatibility target
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[jwt]"
          python -m pip install '${{ matrix.django-spec }}' pytest
      - name: Django integration tests
        run: pytest tests/integration/django_adapter -q


  fastapi-scim-conformance:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - name: Install FastAPI SCIM compatibility target
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[fastapi]"
          python -m pip install httpx pytest
      - name: FastAPI SCIM integration tests
        run: pytest tests/integration/fastapi_scim -q


  scim-provider-profile-conformance:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - name: Install provider-profile test target
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e .
          python -m pip install pytest
      - name: SCIM provider profile tests
        run: pytest tests/unit/provisioning/test_provider_profiles.py -q
